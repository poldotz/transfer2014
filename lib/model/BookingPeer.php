<?php



/**
 * Skeleton subclass for performing query and update operations on the 'booking' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * Thu Apr 24 17:16:03 2014
 *
 * You should add additional methods to this class to meet the
 * application requirements.  This class will only be generated as
 * long as it does not already exist in the output directory.
 *
 * @package    propel.generator.lib.model
 */
class BookingPeer extends BaseBookingPeer
{
    public static function doSelectPager($page=1, $item_per_page = 10, Criteria $criteria = null)
    {
        if ($criteria === null)
        {
            $criteria = new Criteria();
        }
        $pager = new sfPropelPager('Booking', $item_per_page);
        $pager->setCriteria($criteria);
        $pager->setPage($page);
        $pager->setPeerMethod('doSelect');
        $pager->init();
        return $pager;
    }

    public static function getBookingByIdNumber($number,$year){
        $c = new Criteria();
        $c->add(BookingPeer::NUMBER,$number,Criteria::EQUAL);
        $c->add(BookingPeer::YEAR,$year,Criteria::EQUAL);
        return BookingPeer::doSelectOne($c);
    }
    /*
     * Restituisce il numero progressivo relativo ad un anno.
     */
    public static function getIdentificationNumber($year){
        $query = BookingQuery::create()->select(array('number'))->where('Booking.year = ?',$year)->orderByNumber('desc');
        $number = $query->findOne();
        if($query->count()){
            return ($number+1);
        }
        else{
            return 1;
        }
    }
}
