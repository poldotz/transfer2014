<?php


/**
 * Base class that represents a row from the 'vehicle' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * Tue 13 May 2014 04:36:05 PM CEST
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseVehicle extends BaseObject implements Persistent
{
    /**
     * Peer class name
     */
    const PEER = 'VehiclePeer';

    /**
     * The Peer class.
     * Instance provides a convenient way of calling static methods on a class
     * that calling code may not be able to identify.
     * @var        VehiclePeer
     */
    protected static $peer;

    /**
     * The flag var to prevent infinit loop in deep copy
     * @var       boolean
     */
    protected $startCopy = false;

    /**
     * The value for the id field.
     * @var        int
     */
    protected $id;

    /**
     * The value for the vehicle_type_id field.
     * @var        int
     */
    protected $vehicle_type_id;

    /**
     * The value for the model field.
     * @var        string
     */
    protected $model;

    /**
     * The value for the plate_number field.
     * @var        string
     */
    protected $plate_number;

    /**
     * The value for the frame_number field.
     * @var        string
     */
    protected $frame_number;

    /**
     * The value for the mileage field.
     * @var        string
     */
    protected $mileage;

    /**
     * The value for the note field.
     * @var        string
     */
    protected $note;

    /**
     * The value for the created_at field.
     * @var        string
     */
    protected $created_at;

    /**
     * The value for the updated_at field.
     * @var        string
     */
    protected $updated_at;

    /**
     * @var        VehicleType
     */
    protected $aVehicleType;

    /**
     * @var        PropelObjectCollection|Arrival[] Collection to store aggregation of Arrival objects.
     */
    protected $collArrivals;
    protected $collArrivalsPartial;

    /**
     * @var        PropelObjectCollection|Departure[] Collection to store aggregation of Departure objects.
     */
    protected $collDepartures;
    protected $collDeparturesPartial;

    /**
     * Flag to prevent endless save loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInSave = false;

    /**
     * Flag to prevent endless validation loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInValidation = false;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $arrivalsScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $departuresScheduledForDeletion = null;

    /**
     * Get the [id] column value.
     *
     * @return int
     */
    public function getId()
    {
        return $this->id;
    }

    /**
     * Get the [vehicle_type_id] column value.
     *
     * @return int
     */
    public function getVehicleTypeId()
    {
        return $this->vehicle_type_id;
    }

    /**
     * Get the [model] column value.
     *
     * @return string
     */
    public function getModel()
    {
        return $this->model;
    }

    /**
     * Get the [plate_number] column value.
     *
     * @return string
     */
    public function getPlateNumber()
    {
        return $this->plate_number;
    }

    /**
     * Get the [frame_number] column value.
     *
     * @return string
     */
    public function getFrameNumber()
    {
        return $this->frame_number;
    }

    /**
     * Get the [mileage] column value.
     *
     * @return string
     */
    public function getMileage()
    {
        return $this->mileage;
    }

    /**
     * Get the [note] column value.
     *
     * @return string
     */
    public function getNote()
    {
        return $this->note;
    }

    /**
     * Get the [optionally formatted] temporal [created_at] column value.
     *
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null, and 0 if column value is 0000-00-00 00:00:00
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getCreatedAt($format = 'Y-m-d H:i:s')
    {
        if ($this->created_at === null) {
            return null;
        }

        if ($this->created_at === '0000-00-00 00:00:00') {
            // while technically this is not a default value of null,
            // this seems to be closest in meaning.
            return null;
        } else {
            try {
                $dt = new DateTime($this->created_at);
            } catch (Exception $x) {
                throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->created_at, true), $x);
            }
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        } elseif (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        } else {
            return $dt->format($format);
        }
    }

    /**
     * Get the [optionally formatted] temporal [updated_at] column value.
     *
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null, and 0 if column value is 0000-00-00 00:00:00
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getUpdatedAt($format = 'Y-m-d H:i:s')
    {
        if ($this->updated_at === null) {
            return null;
        }

        if ($this->updated_at === '0000-00-00 00:00:00') {
            // while technically this is not a default value of null,
            // this seems to be closest in meaning.
            return null;
        } else {
            try {
                $dt = new DateTime($this->updated_at);
            } catch (Exception $x) {
                throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->updated_at, true), $x);
            }
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        } elseif (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        } else {
            return $dt->format($format);
        }
    }

    /**
     * Set the value of [id] column.
     *
     * @param int $v new value
     * @return Vehicle The current object (for fluent API support)
     */
    public function setId($v)
    {
        if ($v !== null) {
            $v = (int) $v;
        }

        if ($this->id !== $v) {
            $this->id = $v;
            $this->modifiedColumns[] = VehiclePeer::ID;
        }


        return $this;
    } // setId()

    /**
     * Set the value of [vehicle_type_id] column.
     *
     * @param int $v new value
     * @return Vehicle The current object (for fluent API support)
     */
    public function setVehicleTypeId($v)
    {
        if ($v !== null) {
            $v = (int) $v;
        }

        if ($this->vehicle_type_id !== $v) {
            $this->vehicle_type_id = $v;
            $this->modifiedColumns[] = VehiclePeer::VEHICLE_TYPE_ID;
        }

        if ($this->aVehicleType !== null && $this->aVehicleType->getId() !== $v) {
            $this->aVehicleType = null;
        }


        return $this;
    } // setVehicleTypeId()

    /**
     * Set the value of [model] column.
     *
     * @param string $v new value
     * @return Vehicle The current object (for fluent API support)
     */
    public function setModel($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->model !== $v) {
            $this->model = $v;
            $this->modifiedColumns[] = VehiclePeer::MODEL;
        }


        return $this;
    } // setModel()

    /**
     * Set the value of [plate_number] column.
     *
     * @param string $v new value
     * @return Vehicle The current object (for fluent API support)
     */
    public function setPlateNumber($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->plate_number !== $v) {
            $this->plate_number = $v;
            $this->modifiedColumns[] = VehiclePeer::PLATE_NUMBER;
        }


        return $this;
    } // setPlateNumber()

    /**
     * Set the value of [frame_number] column.
     *
     * @param string $v new value
     * @return Vehicle The current object (for fluent API support)
     */
    public function setFrameNumber($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->frame_number !== $v) {
            $this->frame_number = $v;
            $this->modifiedColumns[] = VehiclePeer::FRAME_NUMBER;
        }


        return $this;
    } // setFrameNumber()

    /**
     * Set the value of [mileage] column.
     *
     * @param string $v new value
     * @return Vehicle The current object (for fluent API support)
     */
    public function setMileage($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->mileage !== $v) {
            $this->mileage = $v;
            $this->modifiedColumns[] = VehiclePeer::MILEAGE;
        }


        return $this;
    } // setMileage()

    /**
     * Set the value of [note] column.
     *
     * @param string $v new value
     * @return Vehicle The current object (for fluent API support)
     */
    public function setNote($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->note !== $v) {
            $this->note = $v;
            $this->modifiedColumns[] = VehiclePeer::NOTE;
        }


        return $this;
    } // setNote()

    /**
     * Sets the value of [created_at] column to a normalized version of the date/time value specified.
     *
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return Vehicle The current object (for fluent API support)
     */
    public function setCreatedAt($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->created_at !== null || $dt !== null) {
            $currentDateAsString = ($this->created_at !== null && $tmpDt = new DateTime($this->created_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d H:i:s') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->created_at = $newDateAsString;
                $this->modifiedColumns[] = VehiclePeer::CREATED_AT;
            }
        } // if either are not null


        return $this;
    } // setCreatedAt()

    /**
     * Sets the value of [updated_at] column to a normalized version of the date/time value specified.
     *
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return Vehicle The current object (for fluent API support)
     */
    public function setUpdatedAt($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->updated_at !== null || $dt !== null) {
            $currentDateAsString = ($this->updated_at !== null && $tmpDt = new DateTime($this->updated_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d H:i:s') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->updated_at = $newDateAsString;
                $this->modifiedColumns[] = VehiclePeer::UPDATED_AT;
            }
        } // if either are not null


        return $this;
    } // setUpdatedAt()

    /**
     * Indicates whether the columns in this object are only set to default values.
     *
     * This method can be used in conjunction with isModified() to indicate whether an object is both
     * modified _and_ has some values set which are non-default.
     *
     * @return boolean Whether the columns in this object are only been set with default values.
     */
    public function hasOnlyDefaultValues()
    {
        // otherwise, everything was equal, so return true
        return true;
    } // hasOnlyDefaultValues()

    /**
     * Hydrates (populates) the object variables with values from the database resultset.
     *
     * An offset (0-based "start column") is specified so that objects can be hydrated
     * with a subset of the columns in the resultset rows.  This is needed, for example,
     * for results of JOIN queries where the resultset row includes columns from two or
     * more tables.
     *
     * @param array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
     * @param int $startcol 0-based offset column which indicates which restultset column to start with.
     * @param boolean $rehydrate Whether this object is being re-hydrated from the database.
     * @return int             next starting column
     * @throws PropelException - Any caught Exception will be rewrapped as a PropelException.
     */
    public function hydrate($row, $startcol = 0, $rehydrate = false)
    {
        try {

            $this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
            $this->vehicle_type_id = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
            $this->model = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
            $this->plate_number = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
            $this->frame_number = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
            $this->mileage = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
            $this->note = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
            $this->created_at = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
            $this->updated_at = ($row[$startcol + 8] !== null) ? (string) $row[$startcol + 8] : null;
            $this->resetModified();

            $this->setNew(false);

            if ($rehydrate) {
                $this->ensureConsistency();
            }

            return $startcol + 9; // 9 = VehiclePeer::NUM_HYDRATE_COLUMNS.

        } catch (Exception $e) {
            throw new PropelException("Error populating Vehicle object", $e);
        }
    }

    /**
     * Checks and repairs the internal consistency of the object.
     *
     * This method is executed after an already-instantiated object is re-hydrated
     * from the database.  It exists to check any foreign keys to make sure that
     * the objects related to the current object are correct based on foreign key.
     *
     * You can override this method in the stub class, but you should always invoke
     * the base method from the overridden method (i.e. parent::ensureConsistency()),
     * in case your model changes.
     *
     * @throws PropelException
     */
    public function ensureConsistency()
    {

        if ($this->aVehicleType !== null && $this->vehicle_type_id !== $this->aVehicleType->getId()) {
            $this->aVehicleType = null;
        }
    } // ensureConsistency

    /**
     * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
     *
     * This will only work if the object has been saved and has a valid primary key set.
     *
     * @param boolean $deep (optional) Whether to also de-associated any related objects.
     * @param PropelPDO $con (optional) The PropelPDO connection to use.
     * @return void
     * @throws PropelException - if this object is deleted, unsaved or doesn't have pk match in db
     */
    public function reload($deep = false, PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("Cannot reload a deleted object.");
        }

        if ($this->isNew()) {
            throw new PropelException("Cannot reload an unsaved object.");
        }

        if ($con === null) {
            $con = Propel::getConnection(VehiclePeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        // We don't need to alter the object instance pool; we're just modifying this instance
        // already in the pool.

        $stmt = VehiclePeer::doSelectStmt($this->buildPkeyCriteria(), $con);
        $row = $stmt->fetch(PDO::FETCH_NUM);
        $stmt->closeCursor();
        if (!$row) {
            throw new PropelException('Cannot find matching row in the database to reload object values.');
        }
        $this->hydrate($row, 0, true); // rehydrate

        if ($deep) {  // also de-associate any related objects?

            $this->aVehicleType = null;
            $this->collArrivals = null;

            $this->collDepartures = null;

        } // if (deep)
    }

    /**
     * Removes this object from datastore and sets delete attribute.
     *
     * @param PropelPDO $con
     * @return void
     * @throws PropelException
     * @throws Exception
     * @see        BaseObject::setDeleted()
     * @see        BaseObject::isDeleted()
     */
    public function delete(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("This object has already been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(VehiclePeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        try {
            $deleteQuery = VehicleQuery::create()
                ->filterByPrimaryKey($this->getPrimaryKey());
            $ret = $this->preDelete($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseVehicle:delete:pre') as $callable)
            {
              if (call_user_func($callable, $this, $con))
              {
                $con->commit();
                return;
              }
            }

            if ($ret) {
                $deleteQuery->delete($con);
                $this->postDelete($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseVehicle:delete:post') as $callable)
                {
                  call_user_func($callable, $this, $con);
                }

                $con->commit();
                $this->setDeleted(true);
            } else {
                $con->commit();
            }
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Persists this object to the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All modified related objects will also be persisted in the doSave()
     * method.  This method wraps all precipitate database operations in a
     * single transaction.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @throws Exception
     * @see        doSave()
     */
    public function save(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("You cannot save an object that has been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(VehiclePeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        $isInsert = $this->isNew();
        try {
            $ret = $this->preSave($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseVehicle:save:pre') as $callable)
            {
              if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
              {
                  $con->commit();
                return $affectedRows;
              }
            }

            // symfony_timestampable behavior
            if ($this->isModified() && !$this->isColumnModified(VehiclePeer::UPDATED_AT))
            {
                $this->setUpdatedAt(time());
            }
            if ($isInsert) {
                $ret = $ret && $this->preInsert($con);
                // Timestampable behavior
                if (!$this->isColumnModified(VehiclePeer::CREATED_AT)) {
                    $this->setCreatedAt(time());
                }
                if (!$this->isColumnModified(VehiclePeer::UPDATED_AT)) {
                    $this->setUpdatedAt(time());
                }
                // symfony_timestampable behavior
                if (!$this->isColumnModified(VehiclePeer::CREATED_AT))
                {
                  $this->setCreatedAt(time());
                }

            } else {
                $ret = $ret && $this->preUpdate($con);
                // Timestampable behavior
                if ($this->isModified() && !$this->isColumnModified(VehiclePeer::UPDATED_AT)) {
                    $this->setUpdatedAt(time());
                }
            }
            if ($ret) {
                $affectedRows = $this->doSave($con);
                if ($isInsert) {
                    $this->postInsert($con);
                } else {
                    $this->postUpdate($con);
                }
                $this->postSave($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseVehicle:save:post') as $callable)
                {
                  call_user_func($callable, $this, $con, $affectedRows);
                }

                VehiclePeer::addInstanceToPool($this);
            } else {
                $affectedRows = 0;
            }
            $con->commit();

            return $affectedRows;
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Performs the work of inserting or updating the row in the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All related objects are also updated in this method.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @see        save()
     */
    protected function doSave(PropelPDO $con)
    {
        $affectedRows = 0; // initialize var to track total num of affected rows
        if (!$this->alreadyInSave) {
            $this->alreadyInSave = true;

            // We call the save method on the following object(s) if they
            // were passed to this object by their coresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aVehicleType !== null) {
                if ($this->aVehicleType->isModified() || $this->aVehicleType->isNew()) {
                    $affectedRows += $this->aVehicleType->save($con);
                }
                $this->setVehicleType($this->aVehicleType);
            }

            if ($this->isNew() || $this->isModified()) {
                // persist changes
                if ($this->isNew()) {
                    $this->doInsert($con);
                } else {
                    $this->doUpdate($con);
                }
                $affectedRows += 1;
                $this->resetModified();
            }

            if ($this->arrivalsScheduledForDeletion !== null) {
                if (!$this->arrivalsScheduledForDeletion->isEmpty()) {
                    foreach ($this->arrivalsScheduledForDeletion as $arrival) {
                        // need to save related object because we set the relation to null
                        $arrival->save($con);
                    }
                    $this->arrivalsScheduledForDeletion = null;
                }
            }

            if ($this->collArrivals !== null) {
                foreach ($this->collArrivals as $referrerFK) {
                    if (!$referrerFK->isDeleted()) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->departuresScheduledForDeletion !== null) {
                if (!$this->departuresScheduledForDeletion->isEmpty()) {
                    foreach ($this->departuresScheduledForDeletion as $departure) {
                        // need to save related object because we set the relation to null
                        $departure->save($con);
                    }
                    $this->departuresScheduledForDeletion = null;
                }
            }

            if ($this->collDepartures !== null) {
                foreach ($this->collDepartures as $referrerFK) {
                    if (!$referrerFK->isDeleted()) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            $this->alreadyInSave = false;

        }

        return $affectedRows;
    } // doSave()

    /**
     * Insert the row in the database.
     *
     * @param PropelPDO $con
     *
     * @throws PropelException
     * @see        doSave()
     */
    protected function doInsert(PropelPDO $con)
    {
        $modifiedColumns = array();
        $index = 0;

        $this->modifiedColumns[] = VehiclePeer::ID;
        if (null !== $this->id) {
            throw new PropelException('Cannot insert a value for auto-increment primary key (' . VehiclePeer::ID . ')');
        }

         // check the columns in natural order for more readable SQL queries
        if ($this->isColumnModified(VehiclePeer::ID)) {
            $modifiedColumns[':p' . $index++]  = '`ID`';
        }
        if ($this->isColumnModified(VehiclePeer::VEHICLE_TYPE_ID)) {
            $modifiedColumns[':p' . $index++]  = '`VEHICLE_TYPE_ID`';
        }
        if ($this->isColumnModified(VehiclePeer::MODEL)) {
            $modifiedColumns[':p' . $index++]  = '`MODEL`';
        }
        if ($this->isColumnModified(VehiclePeer::PLATE_NUMBER)) {
            $modifiedColumns[':p' . $index++]  = '`PLATE_NUMBER`';
        }
        if ($this->isColumnModified(VehiclePeer::FRAME_NUMBER)) {
            $modifiedColumns[':p' . $index++]  = '`FRAME_NUMBER`';
        }
        if ($this->isColumnModified(VehiclePeer::MILEAGE)) {
            $modifiedColumns[':p' . $index++]  = '`MILEAGE`';
        }
        if ($this->isColumnModified(VehiclePeer::NOTE)) {
            $modifiedColumns[':p' . $index++]  = '`NOTE`';
        }
        if ($this->isColumnModified(VehiclePeer::CREATED_AT)) {
            $modifiedColumns[':p' . $index++]  = '`CREATED_AT`';
        }
        if ($this->isColumnModified(VehiclePeer::UPDATED_AT)) {
            $modifiedColumns[':p' . $index++]  = '`UPDATED_AT`';
        }

        $sql = sprintf(
            'INSERT INTO `vehicle` (%s) VALUES (%s)',
            implode(', ', $modifiedColumns),
            implode(', ', array_keys($modifiedColumns))
        );

        try {
            $stmt = $con->prepare($sql);
            foreach ($modifiedColumns as $identifier => $columnName) {
                switch ($columnName) {
                    case '`ID`':
                        $stmt->bindValue($identifier, $this->id, PDO::PARAM_INT);
                        break;
                    case '`VEHICLE_TYPE_ID`':
                        $stmt->bindValue($identifier, $this->vehicle_type_id, PDO::PARAM_INT);
                        break;
                    case '`MODEL`':
                        $stmt->bindValue($identifier, $this->model, PDO::PARAM_STR);
                        break;
                    case '`PLATE_NUMBER`':
                        $stmt->bindValue($identifier, $this->plate_number, PDO::PARAM_STR);
                        break;
                    case '`FRAME_NUMBER`':
                        $stmt->bindValue($identifier, $this->frame_number, PDO::PARAM_STR);
                        break;
                    case '`MILEAGE`':
                        $stmt->bindValue($identifier, $this->mileage, PDO::PARAM_INT);
                        break;
                    case '`NOTE`':
                        $stmt->bindValue($identifier, $this->note, PDO::PARAM_STR);
                        break;
                    case '`CREATED_AT`':
                        $stmt->bindValue($identifier, $this->created_at, PDO::PARAM_STR);
                        break;
                    case '`UPDATED_AT`':
                        $stmt->bindValue($identifier, $this->updated_at, PDO::PARAM_STR);
                        break;
                }
            }
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute INSERT statement [%s]', $sql), $e);
        }

        try {
            $pk = $con->lastInsertId();
        } catch (Exception $e) {
            throw new PropelException('Unable to get autoincrement id.', $e);
        }
        $this->setId($pk);

        $this->setNew(false);
    }

    /**
     * Update the row in the database.
     *
     * @param PropelPDO $con
     *
     * @see        doSave()
     */
    protected function doUpdate(PropelPDO $con)
    {
        $selectCriteria = $this->buildPkeyCriteria();
        $valuesCriteria = $this->buildCriteria();
        BasePeer::doUpdate($selectCriteria, $valuesCriteria, $con);
    }

    /**
     * Array of ValidationFailed objects.
     * @var        array ValidationFailed[]
     */
    protected $validationFailures = array();

    /**
     * Gets any ValidationFailed objects that resulted from last call to validate().
     *
     *
     * @return array ValidationFailed[]
     * @see        validate()
     */
    public function getValidationFailures()
    {
        return $this->validationFailures;
    }

    /**
     * Validates the objects modified field values and all objects related to this table.
     *
     * If $columns is either a column name or an array of column names
     * only those columns are validated.
     *
     * @param mixed $columns Column name or an array of column names.
     * @return boolean Whether all columns pass validation.
     * @see        doValidate()
     * @see        getValidationFailures()
     */
    public function validate($columns = null)
    {
        $res = $this->doValidate($columns);
        if ($res === true) {
            $this->validationFailures = array();

            return true;
        } else {
            $this->validationFailures = $res;

            return false;
        }
    }

    /**
     * This function performs the validation work for complex object models.
     *
     * In addition to checking the current object, all related objects will
     * also be validated.  If all pass then <code>true</code> is returned; otherwise
     * an aggreagated array of ValidationFailed objects will be returned.
     *
     * @param array $columns Array of column names to validate.
     * @return mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
     */
    protected function doValidate($columns = null)
    {
        if (!$this->alreadyInValidation) {
            $this->alreadyInValidation = true;
            $retval = null;

            $failureMap = array();


            // We call the validate method on the following object(s) if they
            // were passed to this object by their coresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aVehicleType !== null) {
                if (!$this->aVehicleType->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aVehicleType->getValidationFailures());
                }
            }


            if (($retval = VehiclePeer::doValidate($this, $columns)) !== true) {
                $failureMap = array_merge($failureMap, $retval);
            }


                if ($this->collArrivals !== null) {
                    foreach ($this->collArrivals as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collDepartures !== null) {
                    foreach ($this->collDepartures as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }


            $this->alreadyInValidation = false;
        }

        return (!empty($failureMap) ? $failureMap : true);
    }

    /**
     * Retrieves a field from the object by name passed in as a string.
     *
     * @param string $name name
     * @param string $type The type of fieldname the $name is of:
     *               one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *               BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *               Defaults to BasePeer::TYPE_PHPNAME
     * @return mixed Value of field.
     */
    public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = VehiclePeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
        $field = $this->getByPosition($pos);

        return $field;
    }

    /**
     * Retrieves a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @return mixed Value of field at $pos
     */
    public function getByPosition($pos)
    {
        switch ($pos) {
            case 0:
                return $this->getId();
                break;
            case 1:
                return $this->getVehicleTypeId();
                break;
            case 2:
                return $this->getModel();
                break;
            case 3:
                return $this->getPlateNumber();
                break;
            case 4:
                return $this->getFrameNumber();
                break;
            case 5:
                return $this->getMileage();
                break;
            case 6:
                return $this->getNote();
                break;
            case 7:
                return $this->getCreatedAt();
                break;
            case 8:
                return $this->getUpdatedAt();
                break;
            default:
                return null;
                break;
        } // switch()
    }

    /**
     * Exports the object as an array.
     *
     * You can specify the key type of the array by passing one of the class
     * type constants.
     *
     * @param     string  $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     *                    BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                    Defaults to BasePeer::TYPE_PHPNAME.
     * @param     boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns. Defaults to true.
     * @param     array $alreadyDumpedObjects List of objects to skip to avoid recursion
     * @param     boolean $includeForeignObjects (optional) Whether to include hydrated related objects. Default to FALSE.
     *
     * @return array an associative array containing the field names (as keys) and field values
     */
    public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true, $alreadyDumpedObjects = array(), $includeForeignObjects = false)
    {
        if (isset($alreadyDumpedObjects['Vehicle'][$this->getPrimaryKey()])) {
            return '*RECURSION*';
        }
        $alreadyDumpedObjects['Vehicle'][$this->getPrimaryKey()] = true;
        $keys = VehiclePeer::getFieldNames($keyType);
        $result = array(
            $keys[0] => $this->getId(),
            $keys[1] => $this->getVehicleTypeId(),
            $keys[2] => $this->getModel(),
            $keys[3] => $this->getPlateNumber(),
            $keys[4] => $this->getFrameNumber(),
            $keys[5] => $this->getMileage(),
            $keys[6] => $this->getNote(),
            $keys[7] => $this->getCreatedAt(),
            $keys[8] => $this->getUpdatedAt(),
        );
        if ($includeForeignObjects) {
            if (null !== $this->aVehicleType) {
                $result['VehicleType'] = $this->aVehicleType->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->collArrivals) {
                $result['Arrivals'] = $this->collArrivals->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collDepartures) {
                $result['Departures'] = $this->collDepartures->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
        }

        return $result;
    }

    /**
     * Sets a field from the object by name passed in as a string.
     *
     * @param string $name peer name
     * @param mixed $value field value
     * @param string $type The type of fieldname the $name is of:
     *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                     Defaults to BasePeer::TYPE_PHPNAME
     * @return void
     */
    public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = VehiclePeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);

        $this->setByPosition($pos, $value);
    }

    /**
     * Sets a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @param mixed $value field value
     * @return void
     */
    public function setByPosition($pos, $value)
    {
        switch ($pos) {
            case 0:
                $this->setId($value);
                break;
            case 1:
                $this->setVehicleTypeId($value);
                break;
            case 2:
                $this->setModel($value);
                break;
            case 3:
                $this->setPlateNumber($value);
                break;
            case 4:
                $this->setFrameNumber($value);
                break;
            case 5:
                $this->setMileage($value);
                break;
            case 6:
                $this->setNote($value);
                break;
            case 7:
                $this->setCreatedAt($value);
                break;
            case 8:
                $this->setUpdatedAt($value);
                break;
        } // switch()
    }

    /**
     * Populates the object using an array.
     *
     * This is particularly useful when populating an object from one of the
     * request arrays (e.g. $_POST).  This method goes through the column
     * names, checking to see whether a matching key exists in populated
     * array. If so the setByName() method is called for that column.
     *
     * You can specify the key type of the array by additionally passing one
     * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     * The default key type is the column's BasePeer::TYPE_PHPNAME
     *
     * @param array  $arr     An array to populate the object from.
     * @param string $keyType The type of keys the array uses.
     * @return void
     */
    public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
    {
        $keys = VehiclePeer::getFieldNames($keyType);

        if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
        if (array_key_exists($keys[1], $arr)) $this->setVehicleTypeId($arr[$keys[1]]);
        if (array_key_exists($keys[2], $arr)) $this->setModel($arr[$keys[2]]);
        if (array_key_exists($keys[3], $arr)) $this->setPlateNumber($arr[$keys[3]]);
        if (array_key_exists($keys[4], $arr)) $this->setFrameNumber($arr[$keys[4]]);
        if (array_key_exists($keys[5], $arr)) $this->setMileage($arr[$keys[5]]);
        if (array_key_exists($keys[6], $arr)) $this->setNote($arr[$keys[6]]);
        if (array_key_exists($keys[7], $arr)) $this->setCreatedAt($arr[$keys[7]]);
        if (array_key_exists($keys[8], $arr)) $this->setUpdatedAt($arr[$keys[8]]);
    }

    /**
     * Build a Criteria object containing the values of all modified columns in this object.
     *
     * @return Criteria The Criteria object containing all modified values.
     */
    public function buildCriteria()
    {
        $criteria = new Criteria(VehiclePeer::DATABASE_NAME);

        if ($this->isColumnModified(VehiclePeer::ID)) $criteria->add(VehiclePeer::ID, $this->id);
        if ($this->isColumnModified(VehiclePeer::VEHICLE_TYPE_ID)) $criteria->add(VehiclePeer::VEHICLE_TYPE_ID, $this->vehicle_type_id);
        if ($this->isColumnModified(VehiclePeer::MODEL)) $criteria->add(VehiclePeer::MODEL, $this->model);
        if ($this->isColumnModified(VehiclePeer::PLATE_NUMBER)) $criteria->add(VehiclePeer::PLATE_NUMBER, $this->plate_number);
        if ($this->isColumnModified(VehiclePeer::FRAME_NUMBER)) $criteria->add(VehiclePeer::FRAME_NUMBER, $this->frame_number);
        if ($this->isColumnModified(VehiclePeer::MILEAGE)) $criteria->add(VehiclePeer::MILEAGE, $this->mileage);
        if ($this->isColumnModified(VehiclePeer::NOTE)) $criteria->add(VehiclePeer::NOTE, $this->note);
        if ($this->isColumnModified(VehiclePeer::CREATED_AT)) $criteria->add(VehiclePeer::CREATED_AT, $this->created_at);
        if ($this->isColumnModified(VehiclePeer::UPDATED_AT)) $criteria->add(VehiclePeer::UPDATED_AT, $this->updated_at);

        return $criteria;
    }

    /**
     * Builds a Criteria object containing the primary key for this object.
     *
     * Unlike buildCriteria() this method includes the primary key values regardless
     * of whether or not they have been modified.
     *
     * @return Criteria The Criteria object containing value(s) for primary key(s).
     */
    public function buildPkeyCriteria()
    {
        $criteria = new Criteria(VehiclePeer::DATABASE_NAME);
        $criteria->add(VehiclePeer::ID, $this->id);

        return $criteria;
    }

    /**
     * Returns the primary key for this object (row).
     * @return int
     */
    public function getPrimaryKey()
    {
        return $this->getId();
    }

    /**
     * Generic method to set the primary key (id column).
     *
     * @param  int $key Primary key.
     * @return void
     */
    public function setPrimaryKey($key)
    {
        $this->setId($key);
    }

    /**
     * Returns true if the primary key for this object is null.
     * @return boolean
     */
    public function isPrimaryKeyNull()
    {

        return null === $this->getId();
    }

    /**
     * Sets contents of passed object to values from current object.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param object $copyObj An object of Vehicle (or compatible) type.
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @param boolean $makeNew Whether to reset autoincrement PKs and make the object new.
     * @throws PropelException
     */
    public function copyInto($copyObj, $deepCopy = false, $makeNew = true)
    {
        $copyObj->setVehicleTypeId($this->getVehicleTypeId());
        $copyObj->setModel($this->getModel());
        $copyObj->setPlateNumber($this->getPlateNumber());
        $copyObj->setFrameNumber($this->getFrameNumber());
        $copyObj->setMileage($this->getMileage());
        $copyObj->setNote($this->getNote());
        $copyObj->setCreatedAt($this->getCreatedAt());
        $copyObj->setUpdatedAt($this->getUpdatedAt());

        if ($deepCopy && !$this->startCopy) {
            // important: temporarily setNew(false) because this affects the behavior of
            // the getter/setter methods for fkey referrer objects.
            $copyObj->setNew(false);
            // store object hash to prevent cycle
            $this->startCopy = true;

            foreach ($this->getArrivals() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addArrival($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getDepartures() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addDeparture($relObj->copy($deepCopy));
                }
            }

            //unflag object copy
            $this->startCopy = false;
        } // if ($deepCopy)

        if ($makeNew) {
            $copyObj->setNew(true);
            $copyObj->setId(NULL); // this is a auto-increment column, so set to default value
        }
    }

    /**
     * Makes a copy of this object that will be inserted as a new row in table when saved.
     * It creates a new object filling in the simple attributes, but skipping any primary
     * keys that are defined for the table.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @return Vehicle Clone of current object.
     * @throws PropelException
     */
    public function copy($deepCopy = false)
    {
        // we use get_class(), because this might be a subclass
        $clazz = get_class($this);
        $copyObj = new $clazz();
        $this->copyInto($copyObj, $deepCopy);

        return $copyObj;
    }

    /**
     * Returns a peer instance associated with this om.
     *
     * Since Peer classes are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     *
     * @return VehiclePeer
     */
    public function getPeer()
    {
        if (self::$peer === null) {
            self::$peer = new VehiclePeer();
        }

        return self::$peer;
    }

    /**
     * Declares an association between this object and a VehicleType object.
     *
     * @param             VehicleType $v
     * @return Vehicle The current object (for fluent API support)
     * @throws PropelException
     */
    public function setVehicleType(VehicleType $v = null)
    {
        if ($v === null) {
            $this->setVehicleTypeId(NULL);
        } else {
            $this->setVehicleTypeId($v->getId());
        }

        $this->aVehicleType = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the VehicleType object, it will not be re-added.
        if ($v !== null) {
            $v->addVehicle($this);
        }


        return $this;
    }


    /**
     * Get the associated VehicleType object
     *
     * @param PropelPDO $con Optional Connection object.
     * @return VehicleType The associated VehicleType object.
     * @throws PropelException
     */
    public function getVehicleType(PropelPDO $con = null)
    {
        if ($this->aVehicleType === null && ($this->vehicle_type_id !== null)) {
            $this->aVehicleType = VehicleTypeQuery::create()->findPk($this->vehicle_type_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aVehicleType->addVehicles($this);
             */
        }

        return $this->aVehicleType;
    }


    /**
     * Initializes a collection based on the name of a relation.
     * Avoids crafting an 'init[$relationName]s' method name
     * that wouldn't work when StandardEnglishPluralizer is used.
     *
     * @param string $relationName The name of the relation to initialize
     * @return void
     */
    public function initRelation($relationName)
    {
        if ('Arrival' == $relationName) {
            $this->initArrivals();
        }
        if ('Departure' == $relationName) {
            $this->initDepartures();
        }
    }

    /**
     * Clears out the collArrivals collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return void
     * @see        addArrivals()
     */
    public function clearArrivals()
    {
        $this->collArrivals = null; // important to set this to null since that means it is uninitialized
        $this->collArrivalsPartial = null;
    }

    /**
     * reset is the collArrivals collection loaded partially
     *
     * @return void
     */
    public function resetPartialArrivals($v = true)
    {
        $this->collArrivalsPartial = $v;
    }

    /**
     * Initializes the collArrivals collection.
     *
     * By default this just sets the collArrivals collection to an empty array (like clearcollArrivals());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initArrivals($overrideExisting = true)
    {
        if (null !== $this->collArrivals && !$overrideExisting) {
            return;
        }
        $this->collArrivals = new PropelObjectCollection();
        $this->collArrivals->setModel('Arrival');
    }

    /**
     * Gets an array of Arrival objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Vehicle is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|Arrival[] List of Arrival objects
     * @throws PropelException
     */
    public function getArrivals($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collArrivalsPartial && !$this->isNew();
        if (null === $this->collArrivals || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collArrivals) {
                // return empty collection
                $this->initArrivals();
            } else {
                $collArrivals = ArrivalQuery::create(null, $criteria)
                    ->filterByVehicle($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collArrivalsPartial && count($collArrivals)) {
                      $this->initArrivals(false);

                      foreach($collArrivals as $obj) {
                        if (false == $this->collArrivals->contains($obj)) {
                          $this->collArrivals->append($obj);
                        }
                      }

                      $this->collArrivalsPartial = true;
                    }

                    return $collArrivals;
                }

                if($partial && $this->collArrivals) {
                    foreach($this->collArrivals as $obj) {
                        if($obj->isNew()) {
                            $collArrivals[] = $obj;
                        }
                    }
                }

                $this->collArrivals = $collArrivals;
                $this->collArrivalsPartial = false;
            }
        }

        return $this->collArrivals;
    }

    /**
     * Sets a collection of Arrival objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $arrivals A Propel collection.
     * @param PropelPDO $con Optional connection object
     */
    public function setArrivals(PropelCollection $arrivals, PropelPDO $con = null)
    {
        $this->arrivalsScheduledForDeletion = $this->getArrivals(new Criteria(), $con)->diff($arrivals);

        foreach ($this->arrivalsScheduledForDeletion as $arrivalRemoved) {
            $arrivalRemoved->setVehicle(null);
        }

        $this->collArrivals = null;
        foreach ($arrivals as $arrival) {
            $this->addArrival($arrival);
        }

        $this->collArrivals = $arrivals;
        $this->collArrivalsPartial = false;
    }

    /**
     * Returns the number of related Arrival objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related Arrival objects.
     * @throws PropelException
     */
    public function countArrivals(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collArrivalsPartial && !$this->isNew();
        if (null === $this->collArrivals || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collArrivals) {
                return 0;
            } else {
                if($partial && !$criteria) {
                    return count($this->getArrivals());
                }
                $query = ArrivalQuery::create(null, $criteria);
                if ($distinct) {
                    $query->distinct();
                }

                return $query
                    ->filterByVehicle($this)
                    ->count($con);
            }
        } else {
            return count($this->collArrivals);
        }
    }

    /**
     * Method called to associate a Arrival object to this object
     * through the Arrival foreign key attribute.
     *
     * @param    Arrival $l Arrival
     * @return Vehicle The current object (for fluent API support)
     */
    public function addArrival(Arrival $l)
    {
        if ($this->collArrivals === null) {
            $this->initArrivals();
            $this->collArrivalsPartial = true;
        }
        if (!$this->collArrivals->contains($l)) { // only add it if the **same** object is not already associated
            $this->doAddArrival($l);
        }

        return $this;
    }

    /**
     * @param	Arrival $arrival The arrival object to add.
     */
    protected function doAddArrival($arrival)
    {
        $this->collArrivals[]= $arrival;
        $arrival->setVehicle($this);
    }

    /**
     * @param	Arrival $arrival The arrival object to remove.
     */
    public function removeArrival($arrival)
    {
        if ($this->getArrivals()->contains($arrival)) {
            $this->collArrivals->remove($this->collArrivals->search($arrival));
            if (null === $this->arrivalsScheduledForDeletion) {
                $this->arrivalsScheduledForDeletion = clone $this->collArrivals;
                $this->arrivalsScheduledForDeletion->clear();
            }
            $this->arrivalsScheduledForDeletion[]= $arrival;
            $arrival->setVehicle(null);
        }
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Vehicle is new, it will return
     * an empty collection; or if this Vehicle has previously
     * been saved, it will retrieve related Arrivals from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Vehicle.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Arrival[] List of Arrival objects
     */
    public function getArrivalsJoinLocalityRelatedByLocalityFrom($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ArrivalQuery::create(null, $criteria);
        $query->joinWith('LocalityRelatedByLocalityFrom', $join_behavior);

        return $this->getArrivals($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Vehicle is new, it will return
     * an empty collection; or if this Vehicle has previously
     * been saved, it will retrieve related Arrivals from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Vehicle.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Arrival[] List of Arrival objects
     */
    public function getArrivalsJoinLocalityRelatedByLocalityTo($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ArrivalQuery::create(null, $criteria);
        $query->joinWith('LocalityRelatedByLocalityTo', $join_behavior);

        return $this->getArrivals($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Vehicle is new, it will return
     * an empty collection; or if this Vehicle has previously
     * been saved, it will retrieve related Arrivals from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Vehicle.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Arrival[] List of Arrival objects
     */
    public function getArrivalsJoinBooking($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ArrivalQuery::create(null, $criteria);
        $query->joinWith('Booking', $join_behavior);

        return $this->getArrivals($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Vehicle is new, it will return
     * an empty collection; or if this Vehicle has previously
     * been saved, it will retrieve related Arrivals from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Vehicle.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Arrival[] List of Arrival objects
     */
    public function getArrivalsJoinPaymentMethod($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ArrivalQuery::create(null, $criteria);
        $query->joinWith('PaymentMethod', $join_behavior);

        return $this->getArrivals($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Vehicle is new, it will return
     * an empty collection; or if this Vehicle has previously
     * been saved, it will retrieve related Arrivals from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Vehicle.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Arrival[] List of Arrival objects
     */
    public function getArrivalsJoinsfGuardUser($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ArrivalQuery::create(null, $criteria);
        $query->joinWith('sfGuardUser', $join_behavior);

        return $this->getArrivals($query, $con);
    }

    /**
     * Clears out the collDepartures collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return void
     * @see        addDepartures()
     */
    public function clearDepartures()
    {
        $this->collDepartures = null; // important to set this to null since that means it is uninitialized
        $this->collDeparturesPartial = null;
    }

    /**
     * reset is the collDepartures collection loaded partially
     *
     * @return void
     */
    public function resetPartialDepartures($v = true)
    {
        $this->collDeparturesPartial = $v;
    }

    /**
     * Initializes the collDepartures collection.
     *
     * By default this just sets the collDepartures collection to an empty array (like clearcollDepartures());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initDepartures($overrideExisting = true)
    {
        if (null !== $this->collDepartures && !$overrideExisting) {
            return;
        }
        $this->collDepartures = new PropelObjectCollection();
        $this->collDepartures->setModel('Departure');
    }

    /**
     * Gets an array of Departure objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Vehicle is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|Departure[] List of Departure objects
     * @throws PropelException
     */
    public function getDepartures($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collDeparturesPartial && !$this->isNew();
        if (null === $this->collDepartures || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collDepartures) {
                // return empty collection
                $this->initDepartures();
            } else {
                $collDepartures = DepartureQuery::create(null, $criteria)
                    ->filterByVehicle($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collDeparturesPartial && count($collDepartures)) {
                      $this->initDepartures(false);

                      foreach($collDepartures as $obj) {
                        if (false == $this->collDepartures->contains($obj)) {
                          $this->collDepartures->append($obj);
                        }
                      }

                      $this->collDeparturesPartial = true;
                    }

                    return $collDepartures;
                }

                if($partial && $this->collDepartures) {
                    foreach($this->collDepartures as $obj) {
                        if($obj->isNew()) {
                            $collDepartures[] = $obj;
                        }
                    }
                }

                $this->collDepartures = $collDepartures;
                $this->collDeparturesPartial = false;
            }
        }

        return $this->collDepartures;
    }

    /**
     * Sets a collection of Departure objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $departures A Propel collection.
     * @param PropelPDO $con Optional connection object
     */
    public function setDepartures(PropelCollection $departures, PropelPDO $con = null)
    {
        $this->departuresScheduledForDeletion = $this->getDepartures(new Criteria(), $con)->diff($departures);

        foreach ($this->departuresScheduledForDeletion as $departureRemoved) {
            $departureRemoved->setVehicle(null);
        }

        $this->collDepartures = null;
        foreach ($departures as $departure) {
            $this->addDeparture($departure);
        }

        $this->collDepartures = $departures;
        $this->collDeparturesPartial = false;
    }

    /**
     * Returns the number of related Departure objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related Departure objects.
     * @throws PropelException
     */
    public function countDepartures(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collDeparturesPartial && !$this->isNew();
        if (null === $this->collDepartures || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collDepartures) {
                return 0;
            } else {
                if($partial && !$criteria) {
                    return count($this->getDepartures());
                }
                $query = DepartureQuery::create(null, $criteria);
                if ($distinct) {
                    $query->distinct();
                }

                return $query
                    ->filterByVehicle($this)
                    ->count($con);
            }
        } else {
            return count($this->collDepartures);
        }
    }

    /**
     * Method called to associate a Departure object to this object
     * through the Departure foreign key attribute.
     *
     * @param    Departure $l Departure
     * @return Vehicle The current object (for fluent API support)
     */
    public function addDeparture(Departure $l)
    {
        if ($this->collDepartures === null) {
            $this->initDepartures();
            $this->collDeparturesPartial = true;
        }
        if (!$this->collDepartures->contains($l)) { // only add it if the **same** object is not already associated
            $this->doAddDeparture($l);
        }

        return $this;
    }

    /**
     * @param	Departure $departure The departure object to add.
     */
    protected function doAddDeparture($departure)
    {
        $this->collDepartures[]= $departure;
        $departure->setVehicle($this);
    }

    /**
     * @param	Departure $departure The departure object to remove.
     */
    public function removeDeparture($departure)
    {
        if ($this->getDepartures()->contains($departure)) {
            $this->collDepartures->remove($this->collDepartures->search($departure));
            if (null === $this->departuresScheduledForDeletion) {
                $this->departuresScheduledForDeletion = clone $this->collDepartures;
                $this->departuresScheduledForDeletion->clear();
            }
            $this->departuresScheduledForDeletion[]= $departure;
            $departure->setVehicle(null);
        }
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Vehicle is new, it will return
     * an empty collection; or if this Vehicle has previously
     * been saved, it will retrieve related Departures from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Vehicle.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Departure[] List of Departure objects
     */
    public function getDeparturesJoinLocalityRelatedByLocalityFrom($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = DepartureQuery::create(null, $criteria);
        $query->joinWith('LocalityRelatedByLocalityFrom', $join_behavior);

        return $this->getDepartures($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Vehicle is new, it will return
     * an empty collection; or if this Vehicle has previously
     * been saved, it will retrieve related Departures from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Vehicle.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Departure[] List of Departure objects
     */
    public function getDeparturesJoinLocalityRelatedByLocalityTo($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = DepartureQuery::create(null, $criteria);
        $query->joinWith('LocalityRelatedByLocalityTo', $join_behavior);

        return $this->getDepartures($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Vehicle is new, it will return
     * an empty collection; or if this Vehicle has previously
     * been saved, it will retrieve related Departures from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Vehicle.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Departure[] List of Departure objects
     */
    public function getDeparturesJoinBooking($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = DepartureQuery::create(null, $criteria);
        $query->joinWith('Booking', $join_behavior);

        return $this->getDepartures($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Vehicle is new, it will return
     * an empty collection; or if this Vehicle has previously
     * been saved, it will retrieve related Departures from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Vehicle.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Departure[] List of Departure objects
     */
    public function getDeparturesJoinPaymentMethod($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = DepartureQuery::create(null, $criteria);
        $query->joinWith('PaymentMethod', $join_behavior);

        return $this->getDepartures($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Vehicle is new, it will return
     * an empty collection; or if this Vehicle has previously
     * been saved, it will retrieve related Departures from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Vehicle.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Departure[] List of Departure objects
     */
    public function getDeparturesJoinsfGuardUser($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = DepartureQuery::create(null, $criteria);
        $query->joinWith('sfGuardUser', $join_behavior);

        return $this->getDepartures($query, $con);
    }

    /**
     * Clears the current object and sets all attributes to their default values
     */
    public function clear()
    {
        $this->id = null;
        $this->vehicle_type_id = null;
        $this->model = null;
        $this->plate_number = null;
        $this->frame_number = null;
        $this->mileage = null;
        $this->note = null;
        $this->created_at = null;
        $this->updated_at = null;
        $this->alreadyInSave = false;
        $this->alreadyInValidation = false;
        $this->clearAllReferences();
        $this->resetModified();
        $this->setNew(true);
        $this->setDeleted(false);
    }

    /**
     * Resets all references to other model objects or collections of model objects.
     *
     * This method is a user-space workaround for PHP's inability to garbage collect
     * objects with circular references (even in PHP 5.3). This is currently necessary
     * when using Propel in certain daemon or large-volumne/high-memory operations.
     *
     * @param boolean $deep Whether to also clear the references on all referrer objects.
     */
    public function clearAllReferences($deep = false)
    {
        if ($deep) {
            if ($this->collArrivals) {
                foreach ($this->collArrivals as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collDepartures) {
                foreach ($this->collDepartures as $o) {
                    $o->clearAllReferences($deep);
                }
            }
        } // if ($deep)

        if ($this->collArrivals instanceof PropelCollection) {
            $this->collArrivals->clearIterator();
        }
        $this->collArrivals = null;
        if ($this->collDepartures instanceof PropelCollection) {
            $this->collDepartures->clearIterator();
        }
        $this->collDepartures = null;
        $this->aVehicleType = null;
    }

    /**
     * return the string representation of this object
     *
     * @return string
     */
    public function __toString()
    {
        return (string) $this->exportTo(VehiclePeer::DEFAULT_STRING_FORMAT);
    }

    /**
     * return true is the object is in saving state
     *
     * @return boolean
     */
    public function isAlreadyInSave()
    {
        return $this->alreadyInSave;
    }

    // Timestampable behavior

    /**
     * Mark the current object so that the update date doesn't get updated during next save
     *
     * @return     Vehicle The current object (for fluent API support)
     */
    public function keepUpdateDateUnchanged()
    {
        $this->modifiedColumns[] = VehiclePeer::UPDATED_AT;

        return $this;
    }

    /**
     * Catches calls to virtual methods
     */
    public function __call($name, $params)
    {

        // symfony_behaviors behavior
        if ($callable = sfMixer::getCallable('BaseVehicle:' . $name))
        {
          array_unshift($params, $this);
          return call_user_func_array($callable, $params);
        }


        return parent::__call($name, $params);
    }

}
