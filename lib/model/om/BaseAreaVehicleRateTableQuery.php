<?php


/**
 * Base class that represents a query for the 'area_vehicle_rate_table' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * Mon Nov 17 17:31:37 2014
 *
 * @method AreaVehicleRateTableQuery orderByAreaId($order = Criteria::ASC) Order by the area_id column
 * @method AreaVehicleRateTableQuery orderByVehicleTypeId($order = Criteria::ASC) Order by the vehicle_type_id column
 * @method AreaVehicleRateTableQuery orderByCustomerId($order = Criteria::ASC) Order by the customer_id column
 * @method AreaVehicleRateTableQuery orderByCost($order = Criteria::ASC) Order by the cost column
 *
 * @method AreaVehicleRateTableQuery groupByAreaId() Group by the area_id column
 * @method AreaVehicleRateTableQuery groupByVehicleTypeId() Group by the vehicle_type_id column
 * @method AreaVehicleRateTableQuery groupByCustomerId() Group by the customer_id column
 * @method AreaVehicleRateTableQuery groupByCost() Group by the cost column
 *
 * @method AreaVehicleRateTableQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method AreaVehicleRateTableQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method AreaVehicleRateTableQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method AreaVehicleRateTableQuery leftJoinCustomer($relationAlias = null) Adds a LEFT JOIN clause to the query using the Customer relation
 * @method AreaVehicleRateTableQuery rightJoinCustomer($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Customer relation
 * @method AreaVehicleRateTableQuery innerJoinCustomer($relationAlias = null) Adds a INNER JOIN clause to the query using the Customer relation
 *
 * @method AreaVehicleRateTableQuery leftJoinArea($relationAlias = null) Adds a LEFT JOIN clause to the query using the Area relation
 * @method AreaVehicleRateTableQuery rightJoinArea($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Area relation
 * @method AreaVehicleRateTableQuery innerJoinArea($relationAlias = null) Adds a INNER JOIN clause to the query using the Area relation
 *
 * @method AreaVehicleRateTableQuery leftJoinVehicleType($relationAlias = null) Adds a LEFT JOIN clause to the query using the VehicleType relation
 * @method AreaVehicleRateTableQuery rightJoinVehicleType($relationAlias = null) Adds a RIGHT JOIN clause to the query using the VehicleType relation
 * @method AreaVehicleRateTableQuery innerJoinVehicleType($relationAlias = null) Adds a INNER JOIN clause to the query using the VehicleType relation
 *
 * @method AreaVehicleRateTable findOne(PropelPDO $con = null) Return the first AreaVehicleRateTable matching the query
 * @method AreaVehicleRateTable findOneOrCreate(PropelPDO $con = null) Return the first AreaVehicleRateTable matching the query, or a new AreaVehicleRateTable object populated from the query conditions when no match is found
 *
 * @method AreaVehicleRateTable findOneByAreaId(int $area_id) Return the first AreaVehicleRateTable filtered by the area_id column
 * @method AreaVehicleRateTable findOneByVehicleTypeId(int $vehicle_type_id) Return the first AreaVehicleRateTable filtered by the vehicle_type_id column
 * @method AreaVehicleRateTable findOneByCustomerId(int $customer_id) Return the first AreaVehicleRateTable filtered by the customer_id column
 * @method AreaVehicleRateTable findOneByCost(string $cost) Return the first AreaVehicleRateTable filtered by the cost column
 *
 * @method array findByAreaId(int $area_id) Return AreaVehicleRateTable objects filtered by the area_id column
 * @method array findByVehicleTypeId(int $vehicle_type_id) Return AreaVehicleRateTable objects filtered by the vehicle_type_id column
 * @method array findByCustomerId(int $customer_id) Return AreaVehicleRateTable objects filtered by the customer_id column
 * @method array findByCost(string $cost) Return AreaVehicleRateTable objects filtered by the cost column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseAreaVehicleRateTableQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseAreaVehicleRateTableQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = 'propel', $modelName = 'AreaVehicleRateTable', $modelAlias = null)
    {
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new AreaVehicleRateTableQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param     AreaVehicleRateTableQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return AreaVehicleRateTableQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof AreaVehicleRateTableQuery) {
            return $criteria;
        }
        $query = new AreaVehicleRateTableQuery();
        if (null !== $modelAlias) {
            $query->setModelAlias($modelAlias);
        }
        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj = $c->findPk(array(12, 34, 56), $con);
     * </code>
     *
     * @param array $key Primary key to use for the query
                         A Primary key composition: [$area_id, $vehicle_type_id, $customer_id]
     * @param     PropelPDO $con an optional connection object
     *
     * @return   AreaVehicleRateTable|AreaVehicleRateTable[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = AreaVehicleRateTablePeer::getInstanceFromPool(serialize(array((string) $key[0], (string) $key[1], (string) $key[2]))))) && !$this->formatter) {
            // the object is alredy in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(AreaVehicleRateTablePeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return   AreaVehicleRateTable A model object, or null if the key is not found
     * @throws   PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT `AREA_ID`, `VEHICLE_TYPE_ID`, `CUSTOMER_ID`, `COST` FROM `area_vehicle_rate_table` WHERE `AREA_ID` = :p0 AND `VEHICLE_TYPE_ID` = :p1 AND `CUSTOMER_ID` = :p2';
        try {
            $stmt = $con->prepare($sql);
            $stmt->bindValue(':p0', $key[0], PDO::PARAM_INT);
            $stmt->bindValue(':p1', $key[1], PDO::PARAM_INT);
            $stmt->bindValue(':p2', $key[2], PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new AreaVehicleRateTable();
            $obj->hydrate($row);
            AreaVehicleRateTablePeer::addInstanceToPool($obj, serialize(array((string) $key[0], (string) $key[1], (string) $key[2])));
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return AreaVehicleRateTable|AreaVehicleRateTable[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(array(12, 56), array(832, 123), array(123, 456)), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|AreaVehicleRateTable[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return AreaVehicleRateTableQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {
        $this->addUsingAlias(AreaVehicleRateTablePeer::AREA_ID, $key[0], Criteria::EQUAL);
        $this->addUsingAlias(AreaVehicleRateTablePeer::VEHICLE_TYPE_ID, $key[1], Criteria::EQUAL);
        $this->addUsingAlias(AreaVehicleRateTablePeer::CUSTOMER_ID, $key[2], Criteria::EQUAL);

        return $this;
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return AreaVehicleRateTableQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {
        if (empty($keys)) {
            return $this->add(null, '1<>1', Criteria::CUSTOM);
        }
        foreach ($keys as $key) {
            $cton0 = $this->getNewCriterion(AreaVehicleRateTablePeer::AREA_ID, $key[0], Criteria::EQUAL);
            $cton1 = $this->getNewCriterion(AreaVehicleRateTablePeer::VEHICLE_TYPE_ID, $key[1], Criteria::EQUAL);
            $cton0->addAnd($cton1);
            $cton2 = $this->getNewCriterion(AreaVehicleRateTablePeer::CUSTOMER_ID, $key[2], Criteria::EQUAL);
            $cton0->addAnd($cton2);
            $this->addOr($cton0);
        }

        return $this;
    }

    /**
     * Filter the query on the area_id column
     *
     * Example usage:
     * <code>
     * $query->filterByAreaId(1234); // WHERE area_id = 1234
     * $query->filterByAreaId(array(12, 34)); // WHERE area_id IN (12, 34)
     * $query->filterByAreaId(array('min' => 12)); // WHERE area_id > 12
     * </code>
     *
     * @see       filterByArea()
     *
     * @param     mixed $areaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return AreaVehicleRateTableQuery The current query, for fluid interface
     */
    public function filterByAreaId($areaId = null, $comparison = null)
    {
        if (is_array($areaId) && null === $comparison) {
            $comparison = Criteria::IN;
        }

        return $this->addUsingAlias(AreaVehicleRateTablePeer::AREA_ID, $areaId, $comparison);
    }

    /**
     * Filter the query on the vehicle_type_id column
     *
     * Example usage:
     * <code>
     * $query->filterByVehicleTypeId(1234); // WHERE vehicle_type_id = 1234
     * $query->filterByVehicleTypeId(array(12, 34)); // WHERE vehicle_type_id IN (12, 34)
     * $query->filterByVehicleTypeId(array('min' => 12)); // WHERE vehicle_type_id > 12
     * </code>
     *
     * @see       filterByVehicleType()
     *
     * @param     mixed $vehicleTypeId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return AreaVehicleRateTableQuery The current query, for fluid interface
     */
    public function filterByVehicleTypeId($vehicleTypeId = null, $comparison = null)
    {
        if (is_array($vehicleTypeId) && null === $comparison) {
            $comparison = Criteria::IN;
        }

        return $this->addUsingAlias(AreaVehicleRateTablePeer::VEHICLE_TYPE_ID, $vehicleTypeId, $comparison);
    }

    /**
     * Filter the query on the customer_id column
     *
     * Example usage:
     * <code>
     * $query->filterByCustomerId(1234); // WHERE customer_id = 1234
     * $query->filterByCustomerId(array(12, 34)); // WHERE customer_id IN (12, 34)
     * $query->filterByCustomerId(array('min' => 12)); // WHERE customer_id > 12
     * </code>
     *
     * @see       filterByCustomer()
     *
     * @param     mixed $customerId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return AreaVehicleRateTableQuery The current query, for fluid interface
     */
    public function filterByCustomerId($customerId = null, $comparison = null)
    {
        if (is_array($customerId) && null === $comparison) {
            $comparison = Criteria::IN;
        }

        return $this->addUsingAlias(AreaVehicleRateTablePeer::CUSTOMER_ID, $customerId, $comparison);
    }

    /**
     * Filter the query on the cost column
     *
     * Example usage:
     * <code>
     * $query->filterByCost(1234); // WHERE cost = 1234
     * $query->filterByCost(array(12, 34)); // WHERE cost IN (12, 34)
     * $query->filterByCost(array('min' => 12)); // WHERE cost > 12
     * </code>
     *
     * @param     mixed $cost The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return AreaVehicleRateTableQuery The current query, for fluid interface
     */
    public function filterByCost($cost = null, $comparison = null)
    {
        if (is_array($cost)) {
            $useMinMax = false;
            if (isset($cost['min'])) {
                $this->addUsingAlias(AreaVehicleRateTablePeer::COST, $cost['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($cost['max'])) {
                $this->addUsingAlias(AreaVehicleRateTablePeer::COST, $cost['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(AreaVehicleRateTablePeer::COST, $cost, $comparison);
    }

    /**
     * Filter the query by a related Customer object
     *
     * @param   Customer|PropelObjectCollection $customer The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   AreaVehicleRateTableQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByCustomer($customer, $comparison = null)
    {
        if ($customer instanceof Customer) {
            return $this
                ->addUsingAlias(AreaVehicleRateTablePeer::CUSTOMER_ID, $customer->getId(), $comparison);
        } elseif ($customer instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(AreaVehicleRateTablePeer::CUSTOMER_ID, $customer->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByCustomer() only accepts arguments of type Customer or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Customer relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return AreaVehicleRateTableQuery The current query, for fluid interface
     */
    public function joinCustomer($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Customer');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Customer');
        }

        return $this;
    }

    /**
     * Use the Customer relation Customer object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   CustomerQuery A secondary query class using the current class as primary query
     */
    public function useCustomerQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinCustomer($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Customer', 'CustomerQuery');
    }

    /**
     * Filter the query by a related Area object
     *
     * @param   Area|PropelObjectCollection $area The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   AreaVehicleRateTableQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByArea($area, $comparison = null)
    {
        if ($area instanceof Area) {
            return $this
                ->addUsingAlias(AreaVehicleRateTablePeer::AREA_ID, $area->getId(), $comparison);
        } elseif ($area instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(AreaVehicleRateTablePeer::AREA_ID, $area->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByArea() only accepts arguments of type Area or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Area relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return AreaVehicleRateTableQuery The current query, for fluid interface
     */
    public function joinArea($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Area');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Area');
        }

        return $this;
    }

    /**
     * Use the Area relation Area object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   AreaQuery A secondary query class using the current class as primary query
     */
    public function useAreaQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinArea($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Area', 'AreaQuery');
    }

    /**
     * Filter the query by a related VehicleType object
     *
     * @param   VehicleType|PropelObjectCollection $vehicleType The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   AreaVehicleRateTableQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByVehicleType($vehicleType, $comparison = null)
    {
        if ($vehicleType instanceof VehicleType) {
            return $this
                ->addUsingAlias(AreaVehicleRateTablePeer::VEHICLE_TYPE_ID, $vehicleType->getId(), $comparison);
        } elseif ($vehicleType instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(AreaVehicleRateTablePeer::VEHICLE_TYPE_ID, $vehicleType->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByVehicleType() only accepts arguments of type VehicleType or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the VehicleType relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return AreaVehicleRateTableQuery The current query, for fluid interface
     */
    public function joinVehicleType($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('VehicleType');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'VehicleType');
        }

        return $this;
    }

    /**
     * Use the VehicleType relation VehicleType object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   VehicleTypeQuery A secondary query class using the current class as primary query
     */
    public function useVehicleTypeQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinVehicleType($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'VehicleType', 'VehicleTypeQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   AreaVehicleRateTable $areaVehicleRateTable Object to remove from the list of results
     *
     * @return AreaVehicleRateTableQuery The current query, for fluid interface
     */
    public function prune($areaVehicleRateTable = null)
    {
        if ($areaVehicleRateTable) {
            $this->addCond('pruneCond0', $this->getAliasedColName(AreaVehicleRateTablePeer::AREA_ID), $areaVehicleRateTable->getAreaId(), Criteria::NOT_EQUAL);
            $this->addCond('pruneCond1', $this->getAliasedColName(AreaVehicleRateTablePeer::VEHICLE_TYPE_ID), $areaVehicleRateTable->getVehicleTypeId(), Criteria::NOT_EQUAL);
            $this->addCond('pruneCond2', $this->getAliasedColName(AreaVehicleRateTablePeer::CUSTOMER_ID), $areaVehicleRateTable->getCustomerId(), Criteria::NOT_EQUAL);
            $this->combine(array('pruneCond0', 'pruneCond1', 'pruneCond2'), Criteria::LOGICAL_OR);
        }

        return $this;
    }

}
